import css from "./CartPage.module.css";

export default function CartPage() {
  return (
    <section className={css.pageContainer}>
      <div className={css.container}>Cart Page</div>
    </section>
  );
}
